from setuptools import setup
import os

install_requires = []
# eg install_requires = ['urllib3', 'lxml', 'pytest', 'mock', 'tox', 'pyyaml']


setup(name='personalpreferences',
      version='0.1',
      description='personalpreferences',
      install_requires=install_requires,
      )
