Integration tests for identity.uw using py.test.

Run it by doing something like:

DJANGO_SETTINGS_MODULE=pdp-site.it_settings REMOTE_USER=javerage py.test it

This does make changes on registry eval. Find a more suitable
guinea pig than javerage.